# ====================================
# 🔐 JWT Configuration
# ====================================
# Use a secure key with at least 256 bits (32+ characters recommended)
JWT_SECRET=MySuperSecretKeyThatIsSecureEnough123!

# ====================================
# 🗄️ PostgreSQL Database Configuration
# ====================================
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/tasktracker
SPRING_DATASOURCE_USERNAME=task_user
SPRING_DATASOURCE_PASSWORD=secret123

# ====================================
# 💡 Notes:
# - Rename this file to `.env` to activate.
# - NEVER commit your actual `.env` file with real secrets!
# - Make sure `DotenvInitializer` is enabled in `application.yml`:
#   context:
#     initializer:
#       classes: com.saparbek.Task_Tracker.config.DotenvInitializer
